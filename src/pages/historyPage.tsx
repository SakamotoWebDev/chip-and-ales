// /src/pages/historyPage.tsx
/* page that provides access to the entirety of all games played in the past */
import { useNavigate } from "react-router-dom"


export default function HistoryPage() {
  const navigate = useNavigate()
}